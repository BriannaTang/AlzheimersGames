import React, {Component} from 'react';
import { Text, View, Button, StyleSheet, TouchableOpacity} from 'react-native';

class HangmanAnimation extends Component {
  handleChangeImage = () => {
    
  }
  render() {
    return(
      <View/>
    );
  }
}

export default HangmanAnimation;